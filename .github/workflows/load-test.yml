name: Load Test
on: workflow_dispatch
jobs:
  k6:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: grafana/setup-k6-action@v1
      - run: k6 run -e BASE_URL=https://quiz-server-ma5v.onrender.com .github/load-test.js
